# Template only - do not commit real values. Generate with:
#   kubectl create secret generic supplyiq-secrets \
#     --from-literal=api-auth-token=<real-token>

apiVersion: v1
kind: Secret
metadata:
  name: supplyiq-secrets
type: Opaque
stringData:
  api-auth-token: "REPLACE_ME"
